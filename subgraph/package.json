{
  "name": "Muzu",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ajand/Muzu",
    "create-local": "graph create --node http://localhost:8020/ ajand/Muzu",
    "remove-local": "graph remove --node http://localhost:8020/ ajand/Muzu",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ajand/Muzu"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
